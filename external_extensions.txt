# External Pi extensions to install globally
# One per line. Lines starting with # are comments.
# Formats: npm:<package>, git:<repo>, or local path

# Local clone in extensions/pi-hashline-edit replaces npm:pi-hashline-edit
npm:pi-mcp-adapter
npm:pi-slopchop
git:github.com/lucasmeijer/pi-bash-live-view

